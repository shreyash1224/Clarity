<resources>

    <!-- Base App Theme -->
    <style name="Theme.JournalApp" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/button_brown</item>
        <item name="colorPrimaryVariant">@color/button_light_brown</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="android:colorBackground">@color/beige_background</item>
<!--        <item name="android:fontFamily">@font/roboto</item>-->
        <item name="android:editTextStyle">@style/EditTextStyle</item>
        <item name="android:buttonStyle">@style/ButtonStyle</item>
    </style>

    <!-- EditText Styling -->
    <style name="EditTextStyle">
        <item name="android:background">@drawable/rounded_edittext</item>
        <item name="android:padding">12dp</item>
        <item name="android:textColor">@color/brown_text</item>
        <item name="android:textColorHint">@color/brown_hint</item>
    </style>

    <!-- Button Styling -->
    <style name="ButtonStyle">
        <item name="android:background">@drawable/rounded_button</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:padding">12dp</item>
        <item name="android:textSize">16sp</item>
    </style>

    <!-- Secondary Button (e.g., Create Account) -->
    <style name="ButtonStyle.Secondary">
        <item name="android:background">@drawable/rounded_button_secondary</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

</resources>
